{
  "name": "<%= prompts.extNameSafe.toLocaleLowerCase()%>",
  "version": "0.1.0",
  "description": "<%= prompts.extDescription %>",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Qlik Sense Extension",
    "qliksense-visualization-extension",
    "Qlik Sense",
    "QlikSense",
    "Extension"
  ],
  "author": "<%= prompts.authorName %>",
  "devDependencies": {
    <% if( prompts.useSenseGo ) { %> "sense-go": "^0.3.3", <% } %>
    <% if( prompts.useSenseGo ) { %> "gulp": "git://github.com/gulpjs/gulp#4.0", <% } %>
    "verb": "verbose/verb"
  }
}
